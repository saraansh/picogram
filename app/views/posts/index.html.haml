%h1 Picogram

.posts-wrapper.row
  -@posts.each do |post|
    .post
      .post-head
        .name
          Ben Walker
      .image.center-block
        =link_to (image_tag url_for(post.image)), post_path(post)
      %p.caption
        =post.caption